# PROJECT: HW

PROJECT: HW는 Healing World의 약자로, 전투보다 생활과 꾸미기, 시간의 흐름에 따른 감성 연출을 중심으로 한 힐링 생활형 RPG 웹게임 프로토타입입니다.

## 포함된 기능
- 코인 / 블링 재화 표시
- 브라우저 저장 / 불러오기
- 채집 / 낚시 / 농사 수확 버튼
- 간단한 인벤토리
- 상점 구매
- 하우징 메모
- 실제 시간 기반 시간대 표시
- 시간대별 BGM 제목 전환 구조

## 실행 / 배포 기준
PROJECT: HW는 GitHub Pages에서 구동하는 정적 웹게임을 기준으로 관리합니다.

GitHub Pages 배포 기준:
- 저장소 루트에 `index.html`, `style.css`, `main.js`를 둔다.
- `js/`, `data/`, `assets/` 폴더를 그대로 함께 배포한다.
- GitHub Pages 설정에서 배포 브랜치와 루트 경로를 선택한다.
- 배포 후에는 브라우저 캐시를 피하기 위해 `index.html`의 CSS/JS 버전 쿼리를 갱신한다.

로컬 실행은 필수 운영 방식이 아니라, 배포 전 임시 확인이 필요할 때만 사용합니다.

## 다음 확장 추천
- 실제 오디오 파일 연결
- 하우징 배치 시스템
- NPC 의뢰
- 농사 성장 타이머
- 계절 / 날씨 시스템
- 친구 / 방문 / 채팅 기능

## 폴더 구조
- `index.html`: 메인 페이지
- `style.css`: 전체 스타일
- `main.js`: 초기 부트스트랩
- `js/`: 기능별 스크립트
- `data/`: JSON 게임 데이터
- `assets/`: 오디오/이미지 리소스

## 수정 사항
- `btn-open-inventory` 누락으로 발생하던 초기화 오류 수정
- 인벤토리 버튼 이벤트 바인딩 안전 처리


## 라디오 시스템 추가
- 업로드된 `DIVE DIVERS` 음원을 `assets/audio/radio/dive-divers.mp3`로 포함
- 라디오 재생 / 정지 버튼 추가
- 라디오 재생 중에는 일반 시간대 BGM보다 우선 재생


## 최신 수정
- `Attempted to assign to readonly property` 오류 수정
- `audio.dataset`에 직접 객체를 대입하던 부분을 내부 커스텀 속성(`_hwTrackId`) 사용으로 변경


## 추가 안정화 수정
- `Audio` 객체에 커스텀 속성을 직접 붙이지 않도록 변경
- 현재 로드된 BGM 식별값을 별도 변수로 관리
- Safari / iOS 계열 브라우저에서 발생할 수 있는 읽기 전용 속성 오류 가능성 축소


## 생활 시스템 구체화 업데이트
- 채집 / 낚시 보상을 데이터 테이블 방식으로 변경
- 일반 / 희귀 획득물 추가
- 아이템별 판매가 추가
- 수집품 일괄 판매 기능 추가
- 농사에 씨앗 심기 → 성장 → 수확 단계 추가
- 생활 숙련도와 활동 횟수 표시 추가


## 버튼 호환성 수정
- 예전 농사 버튼 id(`btn-farm`) 참조로 발생하던 초기화 오류 수정
- 현재 농사 수확 버튼 id는 `btn-farm-harvest`
- 버튼 이벤트 연결을 존재 여부 확인 후 바인딩하도록 안정화


## 생활 숙련도 확장 업데이트
- 채집 숙련도 상승 시 희귀 채집물 확률 증가
- 낚시 숙련도 상승 시 획득 수량 및 보너스 코인 증가
- 농사 숙련도 상승 시 성장 시간 단축, 최소/최대 수확량 증가
- 농사 숙련도 5에서 사과 묘목 해금
- 농사 숙련도 10에서 황금 씨앗 해금
- 황금 씨앗 수확 시 황금 사과 추가 획득 가능


## 하우징 실제 배치 시스템 업데이트
- 집 안 4칸 배치 슬롯 추가
- 보유한 가구만 선택 가능
- 1번~4번 칸에 직접 배치 가능
- 배치 상태 저장 / 불러오기 지원
- 배치 초기화 버튼 추가
- 하우징 요약 문구 추가


## 단일 화면 대시보드 개편
- 좌측: 빠른 행동 / 농사 / 시스템 / 설정
- 중앙: 현재 공간(마을/밭/집)과 주요 요약
- 우측: 인벤토리 / 생활 / 하우징 / 상점 탭
- 하단: 알림 로그
- 페이지 이동 없이 한 화면에서 대부분의 기능 확인 가능


## 중앙 화면 감성 강화 업데이트
- 시간대에 따라 중앙 화면 분위기 문구와 그림자 톤 변화
- 마을 / 밭 / 집 상태에 맞는 중앙 공간 상세 설명 추가
- 밭 상태를 중앙 화면에서도 함께 표시
- 집에 배치한 가구를 중앙 화면 칩 형태로 표시
- 단일 화면 구조에서 장면 감성을 더 느낄 수 있도록 개선


## 모바일 UI 최적화 및 카드 텍스트 수정 업데이트
- 집 분위기 카드와 라디오 카드 텍스트가 섞이던 문제 수정
- 모바일에서 우측 탭 패널을 더 쉽게 누를 수 있도록 크기와 배치 조정
- 작은 화면에서 패널 간격, 버튼 높이, 로그 영역 높이 최적화
- 모바일 탭 안내 문구 추가


## 시간대별 배경색 및 장면 변화 업데이트
- 아침 / 낮 / 저녁 / 밤에 따라 중앙 화면 배경색 변화
- 마을 / 밭 / 집 각 공간에 맞는 시간대별 시각 톤 적용
- 현재 시간대와 공간을 함께 보여주는 시각 배지 추가
- 중앙 장면 문구를 시간 흐름에 맞게 보강


## 시간대 배경 가시성 수정
- 기존 hero 배경이 시간대 배경 레이어를 덮던 문제 수정
- 중앙 화면 배경 레이어가 더 확실히 보이도록 투명도와 레이어 우선순위 조정
- 밤 시간대에는 카드와 배지 색도 함께 변화하도록 보강


## Safari 대비 문제 수정
- 밤 시간대에 텍스트가 과도하게 흰색으로 변하던 규칙 제거
- 카드/패널 텍스트 색을 기본 가독성 중심으로 재조정
- 밤 시간대는 배너와 배경만 어둡게 하고, 본문 카드는 밝게 유지하도록 수정


## 시간대 시각 효과 안정화 수정
- 시간대 변화는 중앙 배경 레이어에 집중 적용
- 카드/본문 텍스트 색상은 고정에 가깝게 유지
- Safari에서 대비가 흔들리지 않도록 밤 시간대 카드 밝기 보정
- 시간대별 차이는 배경과 배지에서 더 확실히 보이도록 조정


## 시간대 배경 강한 전환 수정
- 아침 / 낮 / 저녁 / 밤 배경 차이를 더 강한 색상과 그라데이션으로 재구성
- Safari에서도 구분되도록 배경 차이를 의도적으로 크게 조정
- 시간 배지 색상도 시간대별로 달라지도록 보강


## 캐시 무효화 배포 수정
- index.html이 `style.v3.css`, `main.v3.js`를 직접 참조하도록 변경
- JSON 데이터 로드에도 버전 쿼리 추가
- GitHub Pages에서 이전 CSS/JS 캐시가 남는 문제를 줄이기 위한 배포용 수정


## GitHub Pages 클린 배포본
- 중복 파일(`style.v3.css`, `main.v3.js`) 제거
- `index.html`은 `style.css?v=20260412b`, `main.js?v=20260412b`를 참조
- 저장소 루트는 깔끔하게 유지하면서 캐시 무효화도 함께 적용


## 시간대별 배경 시스템 재구현 및 디버그 표시 업데이트
- CSS 클래스 의존도를 줄이고 JS가 중앙 배경을 직접 변경하도록 수정
- 마을 / 밭 / 집별 시간대 배경을 직접 지정
- 현재 적용된 scene/time 값을 화면에 디버그 배지로 표시
- 배포 환경에서 실제 적용 여부를 바로 확인할 수 있도록 개선


## 배경 재구현 포함 GitHub Pages 클린 배포본
- 시간대별 배경 시스템 재구현 + 디버그 표시 포함
- 중복 파일(`style.v3.css`, `main.v3.js`, `DEPLOY_NOTE.txt`) 제거
- `index.html`은 `style.css?v=20260412c`, `main.js?v=20260412c`를 참조
- 저장소 루트는 깔끔하게 유지하면서 캐시 무효화 적용


## 디버그 배지 정상화 및 배경 대비 강화 수정
- DEBUG 배지가 실제 scene/time 값을 표시하도록 보강
- 중앙 배경 색 차이를 더 강한 팔레트로 재조정
- 데이터 속성으로 적용된 scene/time 값을 직접 확인 가능하게 수정


## 시간대별 배경 대비 강화 및 디버그 제거
- 디버그 배지 제거
- 아침 / 낮 / 저녁 / 밤 배경 차이를 더 강한 팔레트로 조정
- 저녁과 밤 배경이 한눈에 구분되도록 색 대비 강화
- 카드 가독성은 유지하면서 중앙 배경 변화는 더 확실하게 보이도록 개선


## 전반적 UI/UX 정리 업데이트
- 중앙 hero 영역을 메인 무대로 재구성
- 좌측 패널을 접이식 시스템/설정 구조로 축약
- 우측 인벤토리 카드 높이와 정보 밀도 조정
- 라디오 버튼 한 줄 정렬 개선
- 로그 영역 크기 축소 및 최근 기록 중심 정리
- 시간대별 배경 대비 강화 유지


## 패널 내부 스크롤 및 로그 접기 업데이트
- 우측 탭 패널 내용을 패널 내부 스크롤 구조로 변경
- 좌측 시스템 / 설정을 기본 접힘 상태로 조정
- 하단 로그를 접기/펼치기 구조로 변경
- 전체 페이지 스크롤보다 패널 내부 정리를 우선하도록 개선


## 전체창 무스크롤 지향 레이아웃 압축 업데이트
- 상단 상태 카드, 중앙 hero, 하단 로그의 세로 높이 전반 축소
- 페이지 전체 스크롤 대신 패널 내부 스크롤 비중 강화
- 접힌 로그 바 높이 최소화
- 한 화면 고정에 더 가깝도록 여백과 패딩을 전반적으로 압축


## 중앙 영역 잘림 수정
- 전체창 고정 상태에서 중앙 hero 콘텐츠가 잘리던 문제 수정
- 중앙 hero 내부만 스크롤 가능하도록 조정
- 하단 라디오 버튼 영역과 로그 바가 겹치지 않도록 여백/높이 보정


## 모바일 안전 동작 복구 버전
- 마지막 정상 동작 버전을 기준으로 복구
- 시간대 / BGM / 상태 갱신 로직은 유지
- 모바일은 JS 구조 변경 없이 CSS 중심으로만 정리
- 기능 안정성을 우선한 안전 버전


## HCSiM형 모바일 안정화 1차
- 하단 고정 네비게이션만 안전하게 복구
- 패널/바텀시트 없이 스크롤 점프 방식으로 동작
- `장면 / 행동 / 가방 / 상점 / 더보기`에서 해당 구역으로 바로 이동
- 기존 정상 동작 로직을 유지하면서 HCSiM형 요소를 작은 단위로 복구


## HCSiM 안정화 리셋 버전
- 마지막 정상 동작 버전을 기준으로 복구
- HCSiM 요소는 하단 고정 네비만 유지
- 패널/시트 실험 로직 제거
- 모바일/iPad 세로에서 장면/행동/가방/상점/더보기로 안전하게 스크롤 이동


## HW 모바일 5패널 재구축 1차
- 모바일 5패널 UI를 기존 대시보드 DOM 위 덧씌우는 방식이 아니라 별도 컨테이너/별도 렌더 흐름으로 재구축
- STATUS / ACTION / BAG / SHOP / LOG를 각각 독립 렌더 함수로 분리
- 하단 탭은 모바일 5패널 컨테이너만 전환
- 데스크톱 레이아웃은 유지하고 모바일에서는 대시보드 본문을 숨김


## 2026-04-15 모바일 5패널 정리
- 구형 모바일 네비(`mobile-hcsim-nav`, `mobile-hcsim-reset-nav`) 제거
- 신형 `mobile-5panel-nav`만 남기도록 `index.html` 정리
- `js/ui.js`에서 구형 모바일 핸들러 제거
- 패널 전환 시 `active`와 실제 `display`를 함께 동기화하도록 보정
- render 이후에도 현재 모바일 패널과 가방 서브탭 상태를 다시 적용하도록 수정
- iPhone 및 iPad 세로 모드에서 모바일 5패널 전환 구조를 우선 적용하도록 CSS 보강

## 개발 방향 참고
- 힐링 생활형 RPG 핵심 시스템 및 개발 로드맵 방향은 별도 설계 문서를 참고


## 2026-04-15b 구조 정리 필수 수정
- 구형 모바일 코드(`mobile-hcsim-*`, `mobile-hcsim-reset-*`)를 비활성화가 아닌 실제 제거 대상으로 정리
- 모바일/데스크톱 장면 전환 선택자를 분리
  - 모바일: `data-m5-view`
  - 데스크톱: `data-desktop-view`
- 모바일 STATUS 패널 렌더가 데스크톱 DOM 텍스트(`sceneDetail`, `lifeSummary`, `homeVisualStatus`)를 참조하지 않도록 수정
- 모바일 전용 렌더 데이터는 상태값과 계산 함수(`getSceneDetailText`, `getLifeSummaryText`, `getHomeSummaryText`, `getBgmLabelText`)로 직접 생성


## PROJECT: HW 협업 작업 규칙

### 공통 목표
- 모바일 구조는 하단 탭 기반 5패널 시스템을 기준으로 개발한다.
- 모바일 패널은 `STATUS`, `ACTION`, `BAG`, `SHOP`, `LOG`로 유지한다.
- 데스크톱은 기존 대시보드형 구조를 유지한다.

### 플랫폼 기준
- iPhone / Android 세로: 모바일 5패널 구조
- iPadOS 세로: 모바일 5패널 구조
- iPadOS 가로: 데스크톱 레이아웃
- PC / Mac 웹: 데스크톱 레이아웃

### 절대 원칙
- 구형 모바일 네비(`mobile-hcsim-nav`, `mobile-hcsim-reset-nav`)는 사용하지 않는다.
- 모바일에서는 신형 5패널 네비만 사용한다.
- 기존 데스크톱 대시보드 DOM을 모바일에 억지로 덧씌우지 않는다.
- 모바일 5패널은 가능한 한 별도 렌더 흐름으로 유지한다.
- 상태 관리 값은 한 곳에서만 관리한다.
- 같은 역할의 네비를 2개 이상 공존시키지 않는다.
- 렌더 함수 안에서 `activePanel`을 강제로 다른 값으로 덮어쓰지 않는다.
- 모바일용 임시 패치 코드를 누적시키지 않는다.
- README 변경 기록 없이 구조를 바꾸지 않는다.

### 상태값 규칙
- 모바일 패널 전환은 `activePanel`만 변경한다.
- 가방 내부 전환은 `activeBagTab`만 변경한다.
- 패널 값은 `status`, `action`, `bag`, `shop`, `log`만 사용한다.
- 가방 탭 값은 `inventory`, `housing`만 사용한다.
- 렌더 함수는 상태를 읽고 DOM을 동기화하되, 임의로 패널 상태를 바꾸지 않는다.

### 파일별 역할
- `index.html`: 모바일 5패널 컨테이너, 하단 탭 버튼, 불필요한 구형 모바일 네비 제거
- `style.css`: 모바일 5패널 표시 규칙, iPhone / iPad 세로 반응형, 데스크톱/모바일 표시 분리
- `js/ui.js`: 패널 전환 로직, 모바일 상태 동기화, 5패널 렌더 함수 연결
- `README.md`: 수정 내역, 해결한 문제, 구조 변경 기록

### 작업 순서
1. 구형 모바일 코드 제거
2. 모바일 5패널 네비 단일화
3. `STATUS` / `ACTION` / `BAG` / `SHOP` / `LOG` 전환 확인
4. `BAG` 서브탭 확인
5. iPhone 세로 확인
6. iPad 세로 확인
7. iPad 가로 데스크톱 유지 확인
8. README 기록
9. GitHub Pages 재배포

### 테스트 체크리스트
- 첫 로딩 시 시간 / 코인 / 블링 / BGM 정상 표시
- 콘솔 오류 없음
- 패널 첫 진입 시 빈 화면 없음
- 장면 탭에서 `STATUS` 표시
- 행동 탭에서 `ACTION` 표시
- 가방 탭에서 `BAG` 표시
- 상점 탭에서 `SHOP` 표시
- 기록 탭에서 `LOG` 표시
- BAG의 인벤토리 / 하우징 전환 정상
- 하우징 배치 버튼 정상 동작
- iPhone 세로 정상
- iPad 세로 정상
- iPad 가로 데스크톱 유지

### 최종 협업 원칙
- 모바일은 5패널 단일 구조로, 데스크톱은 기존 대시보드 구조로 유지하며, 구형 모바일 코드와 임시 패치는 남기지 않는다.


## 2026-04-15c 모바일 5패널 협업 규칙 반영
- 수정 목적: 모바일 5패널 단일 구조와 협업 규칙을 실제 코드 기준으로 정리
- 수정 파일: `index.html`, `main.js`, `style.css`, `js/ui.js`, `js/shop.js`, `data/bgmSchedule.json`, `README.md`
- 해결한 문제
  - `js/ui.js`의 함수 닫힘 오류와 모바일 이벤트 블록 중괄호 오류 수정
  - 모바일 상태명을 `activePanel`, `activeBagTab`으로 단일화
  - 렌더 함수가 패널 상태를 덮어쓰지 않고 현재 상태를 DOM에 적용하도록 정리
  - 모바일 행동 / 농사 / 하우징 / 상점 버튼이 데스크톱 DOM 버튼을 대신 클릭하지 않고 기능 로직을 직접 호출하도록 변경
  - 모바일 CSS의 선택자 누락 구간을 `mobile-5panel-*` 기준으로 복구
  - 실제 BGM 파일이 없는 프로토타입 상태에 맞춰 시간대별 BGM 파일 경로를 `null`로 정리해 404 요청을 방지
  - 빈 favicon 링크를 추가해 기본 `/favicon.ico` 404 요청을 방지
  - 배포 캐시 확인을 위해 `20260415c` 버전 쿼리로 갱신
- 남은 문제
  - 실제 iPhone / iPad 세로 / iPad 가로 실기기 캡처 확인 필요


## 2026-04-16 GitHub Pages 실행 기준 정리
- 수정 목적: 로컬 서버 실행이 아니라 GitHub Pages 배포본을 기준으로 웹게임을 구동하도록 문서 기준 정리
- 수정 파일: `README.md`, `DEPLOY_NOTE.txt`
- 해결한 문제
  - 실행 방법을 로컬 서버 중심 안내에서 GitHub Pages 배포 기준 안내로 변경
  - 오래된 `style.v3.css`, `main.v3.js` 배포 안내를 현재 파일 구조에 맞게 갱신
  - `zip 재배포` 표현을 `GitHub Pages 재배포`로 정리
- 남은 문제
  - GitHub Pages 실제 URL에서 iPhone / iPad 세로 / iPad 가로 확인 필요


## 2026-04-16a 모바일 5패널 완성도 보강
- 수정 목적: 안정화된 모바일 5패널 구조를 실제 사용하기 편한 정보 밀도와 버튼 배치로 개선
- 수정 파일: `index.html`, `main.js`, `style.css`, `js/ui.js`, `DEPLOY_NOTE.txt`, `README.md`
- 해결한 문제
  - `STATUS` 패널에 재화 / 밭 상태 / 가방 요약을 추가해 첫 진입 정보량 보강
  - `ACTION` 패널을 생활 행동과 농사 영역으로 분리하고 iPhone / iPad 세로 버튼 배치를 조정
  - `BAG` 패널에 보유 수량 / 예상 판매가 / 하우징 배치 수 요약 추가
  - `SHOP` 패널에 보유 재화, 상품 타입, 보유 수량, 구매 가능 상태를 표시
  - `LOG` 패널에 기록 유형 배지와 최근 기록 요약을 추가해 가독성 개선
  - GitHub Pages 배포 캐시 확인을 위해 `20260416a` 버전 쿼리로 갱신
- 남은 문제
  - 실제 GitHub Pages URL에서 iPhone / iPad 세로 / iPad 가로 확인 필요


## 2026-04-16b 결제 선행 설계 문서화
- 수정 목적: 결제 구현 전에 계정, 서버 저장, 재화, 상품, 환불, 복원, 운영 기준을 문서와 스키마로 고정
- 수정 파일: `docs/`, `data/products/`, `README.md`
- 해결한 문제
  - `payment-foundation`, `auth-spec`, `storage-scope`, `currency-spec`로 결제 도입 전제를 정리
  - `data-model`, `product-spec`, `payment-api-spec`로 서버 모델과 결제 API 초안을 정의
  - `refund-policy-draft`, `restore-policy-draft`, `admin-tools-spec`로 운영/CS 기준 초안을 분리
  - 상품 JSON Schema와 예시 상품 데이터를 추가
- 남은 문제
  - 실제 서버 기술 스택, PG 제공자, 약관/법무 검토 후 필드와 정책 확정 필요
