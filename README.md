# PROJECT: HW

PROJECT: HW는 Healing World의 약자로, 전투보다 생활과 꾸미기, 시간의 흐름에 따른 감성 연출을 중심으로 한 힐링 생활형 RPG 웹게임 프로토타입입니다.

## 포함된 기능
- 코인 / 블링 재화 표시
- 로컬 저장 / 불러오기
- 채집 / 낚시 / 농사 수확 버튼
- 간단한 인벤토리
- 상점 구매
- 하우징 메모
- 실제 시간 기반 시간대 표시
- 시간대별 BGM 제목 전환 구조

## 실행 방법
정적 웹서버에서 프로젝트 루트를 열어 실행하는 방식을 권장합니다.

예시:
- VS Code Live Server
- Python 간이 서버

```bash
python -m http.server 8000
```

그 다음 브라우저에서 `http://localhost:8000` 접속

## 배포
GitHub Pages에 그대로 올릴 수 있는 정적 웹 구조입니다.

## 다음 확장 추천
- 실제 오디오 파일 연결
- 하우징 배치 시스템
- NPC 의뢰
- 농사 성장 타이머
- 계절 / 날씨 시스템
- 친구 / 방문 / 채팅 기능

## 폴더 구조
- `index.html`: 메인 페이지
- `style.css`: 전체 스타일
- `main.js`: 초기 부트스트랩
- `js/`: 기능별 스크립트
- `data/`: JSON 게임 데이터
- `assets/`: 오디오/이미지 리소스

## 수정 사항
- `btn-open-inventory` 누락으로 발생하던 초기화 오류 수정
- 인벤토리 버튼 이벤트 바인딩 안전 처리


## 라디오 시스템 추가
- 업로드된 `DIVE DIVERS` 음원을 `assets/audio/radio/dive-divers.mp3`로 포함
- 라디오 재생 / 정지 버튼 추가
- 라디오 재생 중에는 일반 시간대 BGM보다 우선 재생


## 최신 수정
- `Attempted to assign to readonly property` 오류 수정
- `audio.dataset`에 직접 객체를 대입하던 부분을 내부 커스텀 속성(`_hwTrackId`) 사용으로 변경


## 추가 안정화 수정
- `Audio` 객체에 커스텀 속성을 직접 붙이지 않도록 변경
- 현재 로드된 BGM 식별값을 별도 변수로 관리
- Safari / iOS 계열 브라우저에서 발생할 수 있는 읽기 전용 속성 오류 가능성 축소


## 생활 시스템 구체화 업데이트
- 채집 / 낚시 보상을 데이터 테이블 방식으로 변경
- 일반 / 희귀 획득물 추가
- 아이템별 판매가 추가
- 수집품 일괄 판매 기능 추가
- 농사에 씨앗 심기 → 성장 → 수확 단계 추가
- 생활 숙련도와 활동 횟수 표시 추가


## 버튼 호환성 수정
- 예전 농사 버튼 id(`btn-farm`) 참조로 발생하던 초기화 오류 수정
- 현재 농사 수확 버튼 id는 `btn-farm-harvest`
- 버튼 이벤트 연결을 존재 여부 확인 후 바인딩하도록 안정화


## 생활 숙련도 확장 업데이트
- 채집 숙련도 상승 시 희귀 채집물 확률 증가
- 낚시 숙련도 상승 시 획득 수량 및 보너스 코인 증가
- 농사 숙련도 상승 시 성장 시간 단축, 최소/최대 수확량 증가
- 농사 숙련도 5에서 사과 묘목 해금
- 농사 숙련도 10에서 황금 씨앗 해금
- 황금 씨앗 수확 시 황금 사과 추가 획득 가능


## 하우징 실제 배치 시스템 업데이트
- 집 안 4칸 배치 슬롯 추가
- 보유한 가구만 선택 가능
- 1번~4번 칸에 직접 배치 가능
- 배치 상태 저장 / 불러오기 지원
- 배치 초기화 버튼 추가
- 하우징 요약 문구 추가


## 단일 화면 대시보드 개편
- 좌측: 빠른 행동 / 농사 / 시스템 / 설정
- 중앙: 현재 공간(마을/밭/집)과 주요 요약
- 우측: 인벤토리 / 생활 / 하우징 / 상점 탭
- 하단: 알림 로그
- 페이지 이동 없이 한 화면에서 대부분의 기능 확인 가능


## 중앙 화면 감성 강화 업데이트
- 시간대에 따라 중앙 화면 분위기 문구와 그림자 톤 변화
- 마을 / 밭 / 집 상태에 맞는 중앙 공간 상세 설명 추가
- 밭 상태를 중앙 화면에서도 함께 표시
- 집에 배치한 가구를 중앙 화면 칩 형태로 표시
- 단일 화면 구조에서 장면 감성을 더 느낄 수 있도록 개선


## 모바일 UI 최적화 및 카드 텍스트 수정 업데이트
- 집 분위기 카드와 라디오 카드 텍스트가 섞이던 문제 수정
- 모바일에서 우측 탭 패널을 더 쉽게 누를 수 있도록 크기와 배치 조정
- 작은 화면에서 패널 간격, 버튼 높이, 로그 영역 높이 최적화
- 모바일 탭 안내 문구 추가


## 시간대별 배경색 및 장면 변화 업데이트
- 아침 / 낮 / 저녁 / 밤에 따라 중앙 화면 배경색 변화
- 마을 / 밭 / 집 각 공간에 맞는 시간대별 시각 톤 적용
- 현재 시간대와 공간을 함께 보여주는 시각 배지 추가
- 중앙 장면 문구를 시간 흐름에 맞게 보강


## 시간대 배경 가시성 수정
- 기존 hero 배경이 시간대 배경 레이어를 덮던 문제 수정
- 중앙 화면 배경 레이어가 더 확실히 보이도록 투명도와 레이어 우선순위 조정
- 밤 시간대에는 카드와 배지 색도 함께 변화하도록 보강


## Safari 대비 문제 수정
- 밤 시간대에 텍스트가 과도하게 흰색으로 변하던 규칙 제거
- 카드/패널 텍스트 색을 기본 가독성 중심으로 재조정
- 밤 시간대는 배너와 배경만 어둡게 하고, 본문 카드는 밝게 유지하도록 수정


## 시간대 시각 효과 안정화 수정
- 시간대 변화는 중앙 배경 레이어에 집중 적용
- 카드/본문 텍스트 색상은 고정에 가깝게 유지
- Safari에서 대비가 흔들리지 않도록 밤 시간대 카드 밝기 보정
- 시간대별 차이는 배경과 배지에서 더 확실히 보이도록 조정


## 시간대 배경 강한 전환 수정
- 아침 / 낮 / 저녁 / 밤 배경 차이를 더 강한 색상과 그라데이션으로 재구성
- Safari에서도 구분되도록 배경 차이를 의도적으로 크게 조정
- 시간 배지 색상도 시간대별로 달라지도록 보강


## 캐시 무효화 배포 수정
- index.html이 `style.v3.css`, `main.v3.js`를 직접 참조하도록 변경
- JSON 데이터 로드에도 버전 쿼리 추가
- GitHub Pages에서 이전 CSS/JS 캐시가 남는 문제를 줄이기 위한 배포용 수정


## GitHub Pages 클린 배포본
- 중복 파일(`style.v3.css`, `main.v3.js`) 제거
- `index.html`은 `style.css?v=20260412b`, `main.js?v=20260412b`를 참조
- 저장소 루트는 깔끔하게 유지하면서 캐시 무효화도 함께 적용


## 시간대별 배경 시스템 재구현 및 디버그 표시 업데이트
- CSS 클래스 의존도를 줄이고 JS가 중앙 배경을 직접 변경하도록 수정
- 마을 / 밭 / 집별 시간대 배경을 직접 지정
- 현재 적용된 scene/time 값을 화면에 디버그 배지로 표시
- 배포 환경에서 실제 적용 여부를 바로 확인할 수 있도록 개선


## 배경 재구현 포함 GitHub Pages 클린 배포본
- 시간대별 배경 시스템 재구현 + 디버그 표시 포함
- 중복 파일(`style.v3.css`, `main.v3.js`, `DEPLOY_NOTE.txt`) 제거
- `index.html`은 `style.css?v=20260412c`, `main.js?v=20260412c`를 참조
- 저장소 루트는 깔끔하게 유지하면서 캐시 무효화 적용


## 디버그 배지 정상화 및 배경 대비 강화 수정
- DEBUG 배지가 실제 scene/time 값을 표시하도록 보강
- 중앙 배경 색 차이를 더 강한 팔레트로 재조정
- 데이터 속성으로 적용된 scene/time 값을 직접 확인 가능하게 수정


## 시간대별 배경 대비 강화 및 디버그 제거
- 디버그 배지 제거
- 아침 / 낮 / 저녁 / 밤 배경 차이를 더 강한 팔레트로 조정
- 저녁과 밤 배경이 한눈에 구분되도록 색 대비 강화
- 카드 가독성은 유지하면서 중앙 배경 변화는 더 확실하게 보이도록 개선


## 전반적 UI/UX 정리 업데이트
- 중앙 hero 영역을 메인 무대로 재구성
- 좌측 패널을 접이식 시스템/설정 구조로 축약
- 우측 인벤토리 카드 높이와 정보 밀도 조정
- 라디오 버튼 한 줄 정렬 개선
- 로그 영역 크기 축소 및 최근 기록 중심 정리
- 시간대별 배경 대비 강화 유지


## 패널 내부 스크롤 및 로그 접기 업데이트
- 우측 탭 패널 내용을 패널 내부 스크롤 구조로 변경
- 좌측 시스템 / 설정을 기본 접힘 상태로 조정
- 하단 로그를 접기/펼치기 구조로 변경
- 전체 페이지 스크롤보다 패널 내부 정리를 우선하도록 개선
